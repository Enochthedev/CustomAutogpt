{
    "commonWords": ["add", "bring", "join", "hello", "welcome"],
    "namePatterns": [
      {
        "pattern": "\\b[A-Z][a-z]+\\s[A-Z][a-z]+\\b",
        "description": "Two consecutive capitalized words"
      },
      {
        "pattern": "\\b(Mr|Ms|Mrs|Dr)\\.?\\s[A-Z][a-z]+\\b",
        "description": "Title followed by capitalized word"
      }
    ],
    "departmentPatterns": [
      "HR", "Finance", "Sales", "IT", "Operations", "Marketing"
    ]
  }